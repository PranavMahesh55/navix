{
  "installCommand": "npm install",
  "buildCommand": "npm run build -w @navix/shared && npm run build -w @navix/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}
